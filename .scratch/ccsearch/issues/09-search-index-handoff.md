# search output carries the session-id + turn handoff

Status: done

## What to build

Turn search output from a standalone snippet list into the **index half of the search→show workflow** (ADR 0002). Search stays a terse index — one line per Match — and explicitly does **not** grow surrounding context; that is `show`'s job.

- Each Session header leads with the **short session-id** (e.g. first 8 chars of the stem) so it can be copied straight into `show`.
- Each Match line is prefixed with its **turn number** (`[12]`) — the same numbering `show --around` consumes.
- The `… +N more` line becomes an **actionable hint**: `+N more  ›  ccsearch show <id>`.

## Acceptance criteria

- [x] Each Session header shows the short session-id alongside the existing `title · date · branch`
- [x] Each Match line shows its turn number
- [x] The overflow line suggests the concrete `ccsearch show <id>` command
- [x] Snippets stay one line — no surrounding-turn context is added
- [x] Updated `assert_cmd` output tests

## Blocked by

- 05-output-polish
- 07-show-verb-transcript


### 2026-06-02 - Done

> *This was generated by AI during triage.*

Shipped in 8434fb2 / d7d3081 — search output carries the session-id + turn handoff. Status set to `done` retroactively during the tracker-vocabulary cleanup (the work predates the `done` state being added to triage-labels.md).
